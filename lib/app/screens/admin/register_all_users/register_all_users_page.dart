import 'package:flutter/material.dart';
import 'register_all_users_controller.dart';

class RegisterAllUsersPage extends StatelessWidget {
  final RegisterAllUsersController controller = RegisterAllUsersController();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('RegisterAllUsersPage'),
      ),
      body: Container(),
    );
  }
}

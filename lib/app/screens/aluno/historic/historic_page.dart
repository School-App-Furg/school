import 'package:flutter/material.dart';

class HistoricPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('HistoricPagePage'),
      ),
      body: Container(),
    );
  }
}
